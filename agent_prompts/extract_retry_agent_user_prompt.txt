The object you need to correct is 
{current_extract_step}.

The error message is {error_message}.

The overall plan is {overall_plan}. Please correct the error in the current extract step such that the overall plan still makes sense.